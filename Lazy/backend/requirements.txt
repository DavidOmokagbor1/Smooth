# Lazy Backend - Python Dependencies

# FastAPI and server
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# Data validation and settings
pydantic==2.5.3
pydantic-settings==2.1.0

# CORS is handled by FastAPI's built-in CORSMiddleware

# Logging
python-json-logger==2.0.7

# Environment variables
python-dotenv==1.0.0

# AI integrations
openai>=2.8.0  # For Whisper and GPT-4o (upgraded to fix compatibility)
# googlemaps==4.10.0  # For Google Maps API (future)
# requests==2.31.0  # For HTTP requests to AI services (future)

# Database
sqlalchemy==2.0.25
asyncpg==0.29.0  # Async PostgreSQL driver
aiosqlite==0.19.0  # Async SQLite driver (for quick testing)
alembic==1.13.1  # Database migrations

